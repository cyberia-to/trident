# Native assertions — implementation green, CLI acceptance running
Branch feat/0.4-native-assertions starts from constants source0051cd5 in selfhost-0.4-assertions. Constants acceptance/evidence will merge before PR integration. Separate cache/install; default branches unchanged. Canonical reference/self-hosting.md updated first. Attributes/purity/contracts follow this slice; no imports or generated-profile claims.
Implement explicit ASSERT/ASSERT_EQ arity/types before broad Noun ranges, once-only source-order branch emission with InvZero failure and Unit success. Shared halting helper recognizes only resolved BUILTIN ASSERT with literal BOOLEAN false, including grouping. Wire body RETURN/EVAL, terminal-tail exemptions and coverage of nonterminal tails; preserve conservative IF/loop continuation. Do not classify ordinary shadow functions, computed false/assert_eq, or let initializers as terminating.
Validate raw Rust seed agreement, exact arity/types, literal vs computed coverage, final callable/local shadowing, all return types, nested arms and empty/nonempty loops, unreachable direct continuations, once-only traces, left/right traps, independent complete ART1 depth and AST/argument caps. Retain every prior source/resource vector and ART1 identity. Installed fresh JOB corpus + all owner gates/baselines/formal audits before integration; UNKNOWN is not proof. Source/evidence commits each followed by all-three installs. Noun128K unchanged.

Resource repair: block_gen.tail constructs only selected continuation; coverage skips statement scanning after already checked terminal-tail coverage. Original native_function_depth fixture/source/196608 cap retained. Temporary larger-arena measurements removed; old depth gate and initial semantic corpus pass.
Next attributes: signatures.Function own pure flag must survive each declaration, including replaced bodies; preserve existing signature bytes via separately owned per-declaration flags (final design review pending). Optimize signatures.chunk to compare symbols.same(source,name,seq.get(functions,index)) directly: packed name span is already first, so lookup need not reconstruct all signature fields and repack a binding. Same full-name comparator is used by constants; preserve final-name reverse order. Seed names are parse_attribute and is_io_builtin (older plan aliases were inaccurate).

Full source gates:1072 Trident/122 Joy/380 Trisha tests pass with zero Rust warnings;4 existing Trisha ignores;133 fixture rows/43 baselines unchanged.84 formal UNKNOWN. Installed CLI remains in progress; no integration until the complete corpus and post-commit identity checks pass.
