# SH3 native compiler scalars
Next owner Trident feature PR to release/0.4: U32 type, builtin as_u32/as_field/sub and operators < / & used by the actual compiler/native Bytes closure. Specify canonical subset first. Decimal literals stay Field; U32 +/* and Field < stay semantic5. Comparison requires U32, equality equal types; conditions explicitly Field/Bool. Replace headers invalid-type sentinel3 before assigning U32=3. Match seed precedence == < comparison < addition < multiplication < bitwise; do not collide with CALL_MARKER27.
Resolve final user signatures before builtin fallback, preserve callable/local namespace separation, distinguish builtin AST/call frame kind and exclude builtins from graph/table. Reuse owned source-order argument links; validate arity/types. Qualified names remain unsupported until import resolution. as_field is identity; < / & / sub use native10/12/6. as_u32 evaluates once, checks <2^32 and traps with inv(0) outside range, matching seed native/calls.rs; no axis0 crash. Exact final emitted depth includes guard/wrappers.
Acceptance: whole native JOB to separate ART1 plus Rust-seed/independent execution, typed args/returns/locals/mutation/shadowing, user builtin-name overrides/forward bindings, precedence/high-byte masks, wrong arity/types and U32 conditions. Split compile-only test support so conversion overflow is successful compilation followed by runtime failure; zero/max pass,2^32/P-1 trap,P/P+1 normalize first. Test exact/-1 depth/sequence and preserve previous artifact identities, bounded quota failures and old destinations. Rebuild owners, cargo gates, Trisha baseline, formal UNKNOWN honesty, installed CLI, pinned receipts, exact feature PR integration.
After scalars: bounded loops, Noun/aggregates, constants/attributes, imports and whole compiler closure; SH4 source-scale allocation persists independently. Do not claim compiler self-build until C1(S)->C2 and then fixed point gates pass.
