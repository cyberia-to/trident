# True module closure — explicit imports implementation
Seed sourceef4ea23 accepted with Trisha fixturesef186c3; feature fix/0.4-explicit-imports based on1b30f8d (PR90) in selfhost-0.4-constants; completed callable acceptance remains isolated in selfhost-0.4-assertions. Separate caches/install roots; defaults unchanged. Canonical reference/language.md + self-hosting-jobs.md: separate exact MOD1 sources/owners, no source concatenation, no ambient host imports. Current compiler language closure needs qualified calls/constants/types/constructors and bodyless intrinsics; current source does not need cfg/generics/match/asm/dynamic bounded loops. Inventory complete pinned closure before SH4 claims.
1. Accepted seed final active callable prerequisite87f464f: [fix plan](final-callable-exports.md). One final declaration owns visibility/signature/kind/intrinsic/body; native aliases/TIR/specialization/recursion agree. Preserve checking replaced ordinary bodies and owner snapshots.
2. Explicit imports prerequisite: immutable resolved direct-use scope (requested path, canonical owner/index, use span), built once per ParsedModule before checker/specialization and shared by LSP/constants/native/TIR. Preserve resolver key for validation: strict requested=declared owner except canonical documented legacy remaps; entry basename may differ. Resolve aliases before dedup so legacy+canonical same owner resolves once. Replace line header scanner with parser-owned metadata handling one-line/tabs/comments. Rewrite arbitrary early.tri→std.same/late.tri→os.same oracle fixtures to explicit canonical paths/imports preserving31/38 outputs; separately reject arbitrary mismatch. Direct uses bind in source order; per-symbol full/short aliases retain final-binding semantics. All canonical private definitions/layouts remain available to implementation/opaque return values, while visible aliases/constants are rebuilt from direct scope only. Reject transitive/sibling calls/constants/type construction/intrinsic access; adding unrelated module cannot alter caller. Test private generic helpers, opaque nominal forwarding, two same-basename owners/reversed uses and live editor overlays. Source-only File APIs resolve File.uses against supplied owners and reject absent deps.
3. Guest package graph + qualified constants: job.View returns modules, entry index, explicit options and remaining shared admission budget. Scan program/module/use dotted names; resolve exact reachable graph iteratively with lexical order, UTF-8/name checks and missing/cycle diagnostics by original module index/span. First executed fixture imports a public constant; reject private/missing/cycle/mismatch/reachable malformed UTF-8; unused bytes only get structural/size admission.
4. Qualified functions + nominal types: immutable imported bindings, declaration→module ownership and global callable IDs; retain packed signature bytes and original source spans. Full/short aliases resolve to exact IDs before emission; deterministic (full owner,name) ordering. Propagate original nominal owner through public signatures and opaque Seq/Bytes forwarding; validate private helpers, same basename, ordered once-only args, external private construction/read/write rejection.
5. Exact intrinsic declarations: namespace policy and complete target ABI validation separate from reachable lowering. vm.core.convert contains unused split with known ABI but unsupported native lowering; do not reject whole module solely for unreachable split. Imported declared assert alias may produce BUILTIN/halting; wrappers and same basenames do not. Last intrinsic attribute wins as in seed; final declaration owns it. No transitive function_requirements inference of direct identity.
6. Generated1/1 compiler profiles: explicitly pass JOB options to ART1 for Noun→Noun entry; no profile guessing. Keep raw0/0 exact output. job.read/emit.artifact currently reject/hardcode0/0. Execute a freshly generated compiler profile before whole C2/C3 claims.
Cfg later: only explicit OPT1 flags, last cfg wins, inactive items still lex/parse; brace-skipping cannot accept malformed inactive syntax. Until implemented return unsupported6. No hidden debug/release flags.
Shared resource policy: call_graph.walk1024×16 assumes TOTAL source≤4096. Per-module4096 breaks it. First modules slice keeps common reachable-source/work budget or charges shared declaration/edge/AST counter before append. SH4 separately lifts source4096, NONE4096, loop passes, depth128/types64 and measured total arena scale. Keep all old vectors and public quotas until explicit canonical resource revision and executable boundary receipts.

Accepted seed implementation: parser-owned discovery and strict legacy/canonical owners, shared resolved scopes, direct aliases/constants, final struct visibility, source-name guards before lowering, TIR source→canonical AST/layouts, and nox exact type aliases. LSP exact visible maps use live parser headers and recovered incomplete bodies; references/rename remain lexical and are outside this slice. Discovery CLI12positive/15negative projects executed on both warriors, C1 bytes unchanged; [pinned receipt](../../audit/self-hosting/explicit-imports.md):1114/122/380 tests,120 library checks,79 CLI commands,15 direct-TIR executions,133 unchanged baseline rows,90 formal UNKNOWN; all3 binaries/C1 reproduced after source commits. Merge this slice to release/0.4, then start guest graph/constants on its own feature branch. Companion Trisha fixtures now use exact ext/a owners; direct TIR rejects malformed AST before warrior emission, whose independent ERROR-comment guard remains tested. std.fhe.pbs/rlwe now declare vm.core.field directly. Nominal scope design: checker Ty::Struct already embeds owner fields and GenericFnDef owner snapshots; keep its structs map source-visible only. Backends retain canonical closure layouts. TIR must canonicalize source types once in declaring scope, store only canonical var/return/field types and use direct canonical lookup afterward (including generated match bindings). Add per-symbol type aliases beside functions/constants; prefix aliases alone lose earlier non-overridden symbols. Nox canonical field_index is already direct; source qualified_type must use exact type aliases. Include lsp/project.rs collect_project_exports and editor symbol visibility in shared direct scopes. Lexer supports line comments; block comments remain lexical errors.

Active guest work: feat/0.4-guest-modules from Trident PR91 merge815aee5; sibling Trisha PR15 merge5f72e9c, Joyd662ef9 (same tree as release merge). Post-merge all3 installed hashes/C1 unchanged, /tmp/explicit-imports-integration.json. Defaults unchanged. First guest unit retains job.Package modules/read_cost/remaining/source_limit/options in View; find/open consume shared allowance and preserve original package indices. Reference self-hosting-jobs updated first. Component tests and graph/qualified-constant integration in progress; guest imports not yet accepted. Preserve existing single-entry112 fixed validation projections and all old source/arena caps.
Package prerequisite: binary path lookup uses at most17 probes, exact/one-below shared allowance tested;4097-entry admission needed batching terminal tree leaves while preserving exact visit charges. Old collection/admission boundary vectors pass; no limit raised. Opaque bytes.BytesTable owns append-only validated length/root handles, with no raw decode/serialization bypass; graph state will retain separate source/name tables and reference compact indices. Full source corpus and owner gates required before committing this prerequisite; graph/header/constants follow separately.
