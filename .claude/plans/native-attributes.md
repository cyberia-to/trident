# Native function attributes — accepted source and installed corpus
Branch feat/0.4-native-attributes from eca4ea8 (assertions source5730eb8 + merged constants PR87). Reuses clean selfhost-0.4-constants worktree/cache/install after constants integration; assertions CLI is isolated in selfhost-0.4-assertions. Never share target/release between roots. Merge accepted assertions evidence before integration. Canonical reference/self-hosting.md first; Noun128K unchanged.
attributes.tri scans #[] prefixes before optional pub; pure/requires/ensures only. Contracts are lexer-token metadata, balanced round parentheses only, empty/unknown/malformed expressions allowed, no evaluation/typechecking. No expression-stack64 cap. Invalid lexical1, missing delimiters/annotated nonfunction2; HASH outside function-prefix6; unknown/unsupported attribute forms6. Exact asm stays unsupported6 because seed has a special lexer; other valid token13 payloads are accepted. const_parse admits following #.
Keep signatures.Function pack/read bytes unchanged. Headers own sparse increasing declaration IDs for pure functions; append only on completed pure declaration. check_functions consumes IDs with monotone cursor and gives each body its own Bool, including replaced declarations. Body/Expression contexts carry pure; emission does not. Direct I/O name check precedes callable lookup, exactly seed is_io_builtin, including ordinary same-named functions; no transitive effect analysis.
Optimize signatures name-only reverse lookup to symbols.same on packed signature's first span, without reconstructing all fields and a binding. This is a reviewed layout equivalence; all old full-name/final-binding/primitive byte fixtures stay unchanged. Keep every source/resource vector and budget; measure full CLI/caps before accepting any optimization claim.
Test metadata-only ART1 identity, duplicate/empty/nested>64 contracts, token13/asm/invalid/missing delimiters, fn-only and pub placement, flag reset and both replacement orders. Exact I/O names/prefixes vs near misses, ordinary source shadows and transitive helpers, calls in all expression contexts, sparse metadata across header/body chunk boundaries, exact declaration caps. All owner gates, 133 rows/43 baselines, installed fresh JOB corpus, formal UNKNOWN and post-commit binary/C1 identity checks before PR to release/0.4.

Resource repair: check_scope owns immutable pure/type/function/parameter/constant context once per body, shared through expression contexts. body_state.scoped initializes its real start/return/scope directly, avoiding discarded empty scope and persistent rewrites. Original196608 component gate passes; all temporary larger-arena diagnostics removed. Exact11 I/O spellings use readable packed word tables plus explicit prefixes; signature and emitted-artifact contracts unchanged. Read-only review caught HASH diagnostic drift; ast.failed/headers.expected retain unsupported6 outside function prefixes, including const initializers. Acceptance: [pinned receipt](../../audit/self-hosting/native-compiler-attributes.md).

Source gates green:1078 Trident/122 Joy/380 Trisha CPU tests,zero Rust warnings,4 existing Trisha ignores.133 fixture rows/43 baselines unchanged;88 formal UNKNOWN. Source0f54f59 and assertions receipt merge6b6e733:1190 installed commands/401 observations/189 positive cases; all172 prior ART1 identities unchanged. All rebuilt binaries and C1 match executed bytes. Next: seed final-callable ownership, explicit imports, guest module closure and intrinsic ownership; SH4/generated profiles/C2/C3/platforms/proof gates remain open.
