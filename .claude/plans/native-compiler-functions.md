# SH3 native compiler functions

Next delivery after control, feature PR to release/0.4; master unchanged. Contract first: one program, ordinary Field/Bool parameters and Field/Bool/Unit results (Unit = omitted return annotation); main()->Field. Imports/generics/intrinsic declarations remain explicit unsupported6. Collect all signatures/body token spans before checking all bodies, including unused; forward calls resolve independently of locals. Parameter slots preserve source order/arity, repeated names choose the last binding. Same-owner repeated function declarations use seed last-definition callable binding while each active body is checked against its own declared return type; preserve that or explicitly reject unsupported6, never silently redesign seed.

Persist per-function typed AST until every callee frame count is known. Store signature params in one flat source-order Seq (ranges are safe in the nonnested header pass); per-body bindings reset so slot IDs stay function-relative. Store typed body Seq handles via to_noun, revalidate once per emission via from_noun with bounded visits; keep call adjacency as owned Noun lists so graph traversal never repeatedly revalidates whole ASTs. Duplicate final bindings alone contribute to seed recursion detection (typecheck/analysis.rs); every declared body is still type checked. Add iterative call-delimiter state (callee, operand/operator bases, argument count), commas and separate owned argument links; nested arguments are never a contiguous arena range. Argument links use NONE4096, contain [expr_id previous_link] and are reversed per call. Call markers27 share the operator stack; comma/close flush only binary operators and never reduce a call marker. A call frame carries callee/start/operator-base/value-base/count/head. Empty/trailing-comma close is valid only at a call marker, not ordinary parentheses. Evaluate arguments once left-to-right. Build their source-order formula leaves, fold adjacent pairs with CONS and quote(empty(level)) padding; extend to the planned callee frame height without evaluating padded locals. Iterative call-graph cycle checks and reachable planning precede sorted full-name code-table slots. One reusable body per reachable function; immutable balanced table and fresh balanced callee frame, params first then zero locals. Raw apply is [2 new_subject code_producer], with code_producer=axis(2*table_capacity+slot); existing quoted codegen.compose is only for fixed continuations. Preserve prior no-call artifact identities.

Unit requires removing declarations expected_type=2 sentinel, explicit condition Field/Bool checks, and parameterized return/terminal coverage; Unit fallthrough returns atom0 and same-type equality follows seed. Acceptance: distinct helper7/main9, forward/multiple/nested calls and owned argument lists, Bool/Unit, callee return isolation, caller state, function/local name collision, duplicate parameter/function semantics, wrong arity/types/unknown names, direct/mutual/dead-branch cycles, exact collection/table caps and actual dynamic dispatch. Inspect seed recursion reachability before claiming parity. Measure real JOB resource limits; full self-build/SH4 stay open. Relevant seed: typecheck/{file,block,flow}.rs, ir/tree/lower/nox/native/{calls,layout,plan}.rs; reader contracts in reference/self-hosting-{jobs,runtime}.md.
Implementation state: headers, typed call parsing, Unit/body coverage, stored ASTs, sorted table/reachability and fresh-frame dynamic call emission are wired into parser.compile. Graph heights include memoized suffixes; recursion uses semantic5 (JOB1 code4 remains import-cycle). Targeted release tests in progress, including full JOB corpus and exact old artifacts. Next add graph/depth/argument cap probes, installed Joy cases and full cross-repo gates/formal audits, commit/rebuild/receipt/PR to release/0.4. Keep full source/compiler-scale limits and SH4 open; do not equate component boundary probes with admitted full JOBs.
