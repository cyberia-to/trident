# SH4 bounded heap arena

Next delivery after functions: owner feature PRs to nox/Joy release/0.4; no master/main changes. Nox adds a fallible heap constructor for Reduction<N> that initializes in place, never Box::new(Self::new()) or a large temporary. Specify in specs first; keep hash-cons order, allocation limits and codec unchanged. Invalid capacity and allocation failure return explicit errors; small-stack/large-arena and canonical identity tests. Joy uses this constructor in both structured execution and pack construction; generalize execute/construct/encode_job by const capacity. Preserve default196608 and require explicit allowance up to786432, selecting trusted physical tiers2^18/2^20. Count size_of::<Reduction<N>>() in reserved-memory reports; artifact caps and proof profiles remain separate.
Acceptance: same JOB at both physical tiers gives identical result/ART1, reductions and allocated nodes; exact/one-below allowances; previous default failures remain bounded and preserve old outputs. Installed pack→run→RES1→ART1 must succeed for a real native compiler job exceeding196608 nodes. Measure31 assignments,64 nested calls and4096-byte admission. Record all failed drafts; larger prepared component tests are not full JOB acceptance. Rebuild all owners after commits, cross-repo gates/baselines, source pins and feature PRs before integration.
Scope remains a measured SH4 increment. Seed emission,4096-byte guest source ceiling, remaining language (U32, loops, aggregates, imports), full closure, C2/C3, six targets and native Zheng compiler proofs stay open. Avoid GC, automatic fallback, protocol changes or claim of complete self-hosting from raising one cap.
