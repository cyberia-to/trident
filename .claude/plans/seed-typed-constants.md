# Seed typed constants — accepted

Source2f6b0ef; [validation](../../audit/self-hosting/typed-constants.md). Shared typed resolver freezes final active declarations before signatures; every active initializer is checked. Integer literals and exact-type aliases, forward references, iterative cycle checks, raw U32 validation and final visibility. Checker, generic specialization, nox and TIR share resolution. Private dimensions become literal extents; expression constants retain typed owner bindings. Direct TIR builder honors final cfg and explicit external Field bindings.1058/122/380 tests green,133 unchanged fixture rows/43 baselines,61 installed commands/20 cases executed or rejected on both warriors.79 formal UNKNOWN; entire C1 byte-identical to array delivery. General851-command guest corpus not rerun for this seed-only fix.

Use CARGO_TARGET_DIR=/Users/master/cyber/.worktrees/selfhost-0.4-constants/target. This isolated root has own Trident/Joy/Trisha worktrees, copied Trisha vendor and symlinked unchanged clean siblings. Never share target/release binary paths between concurrently edited Trident worktrees: Cargo freshness can retain a sibling worktree's top-level executable. The array delivery was rebuilt after discovery; all three hashes matched its original executed binaries.

Next guest delivery: constants first, then attributes/asserts, then true imports. Existing header scan collects declarations; resolve final environment at EOF before bodies. Keep array extents/loop bounds literal-only in this first increment (actual compiler closure needs no named extents), avoiding a redundant scan of every old source. constants.tri owns declarations/resolution/scope, const_parse.tri owns syntax; no constants in type_registry. Header/body/expression contexts carry immutable scope, emission transport does not.

Record full name span, public/type/initializer metadata; memo side table0 unseen/1 visiting/literal-id+2 resolved. Alias chains share terminal literal provenance. Validate all declarations including replaced; last full-name lookup wins. Scope owns logical module once; future imports must distinguish alias owner from literal origin and never reinterpret a foreign span in caller source. Grouped integer/reference initializers admitted; arithmetic/calls reject wholly.

Use a separate AST CONSTANT: runtime quote matches literal, but index_parse raw-decimal check remains LITERAL-only. Field P via constant therefore indexes0; literal P rejects. Locals/parameters/tuple bindings shadow constants in expressions. U32 raw range uses pre-multiply threshold from loop_parse.bound(...,5), not array_size.decimal with a larger cap. Coverage recognizes known constant conditions. Empty-scope fast path, retain hot State field positions and old196608 quotas (body-chunks194469 nodes at a427531).

Acceptance: forward/duplicate/grouped aliases, final type/public metadata, cycles/unknown/overflow in replaced declarations, U32max/max+1, FieldP/P+1, long common-prefix names, shadowing, constant-vs-literal indexing, exact independent declaration/AST/frame/type caps, old ART1 identities and retained SH4 resource cases.

Later assertions: Bool assert/Field assert_eq ->Unit; ordered once-only arguments. Only resolved direct builtin assert(false) Expr/Return contributes termination; user functions shadow it, assert_eq(0,1)/assert(1==0)/let x=assert(false) do not follow that seed shortcut. Update body/terminal/coverage together. Attributes pure/requires/ensures need bounded valid syntax, no implicit runtime enforcement; pure also rejects seed I/O names, even same-named ordinary functions. True imports must preserve exact intrinsic declaration ownership.
