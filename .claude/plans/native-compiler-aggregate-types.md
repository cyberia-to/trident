# SH3 aggregate descriptors, Digest and tuples
Feature PRs into release/0.4. Preserve primitive program ART1 identities and the raw/structured entry ABIs. Canonical descriptors, Digest and source tuples/destructuring are accepted. Nominal structs, persistent field writes, arrays and real imports still follow; these deliveries do not close SH3.

## Canonical descriptors — accepted

Internal foundation accepted at `adbb00e`; [validation](../../audit/self-hosting/native-compiler-types.md). Digest type, native identity, equality and checked Field/U32 indexing accepted at `dbf3c13`; [execution evidence](../../audit/self-hosting/native-compiler-digest.md). Tuple annotations/values, typed functions and ordered flat destructuring accepted at `850525c`; [validation](../../audit/self-hosting/native-compiler-tuples.md). Repeated struct initializers reject at `ac2481d`; [seed validation](../../audit/self-hosting/unique-struct-initializers.md). Next: nominal structs and field reads.
Represent types as canonical Noun values, with current primitive tags remaining atoms. Primitive AST/signature/binding records therefore preserve their bytes. Composite descriptors carry kind, ordered children, derived logical depth and contains_noun. Equality uses native identity of canonical descriptors; primitive tests compare with explicit tag atoms. No mutable type interner or O(n²) search, no evolving registry through every body.

Move type ownership into types.tri; migrate AST type access, signatures, bindings, body result and builtins from Field to Noun. Primitive tags have one owner in types.tri; packed records insert complete descriptors without atom wrapping. Primitive construction/equality uses direct noun intrinsics so the guest compiler avoids an extra function invocation per check. Invalid type has a unique descriptor distinct from all valid primitives/composites. Keep token recognition and all current accepted syntax unchanged in the initial refactor. Component tests assert primitive record byte preservation, exact descriptor equality and bounded composite construction. Full old JOB/ART1 corpus must preserve outputs/identities. Formally audited UNKNOWN is still not proof.

Composite constructors use bounded child sequences and explicit logical nesting/node limits derived from job capacity and parser STACK_CAP. Derived depth/contains_noun must come from trusted child descriptors; never accept caller-supplied metadata. Nominal struct identity later includes the defining module, not structural equivalence. Fixed arrays later carry element type and checked extent. Type bounds, output formula depth and execution arena remain independent.

## Digest and tuple language increments
Digest is its own primitive descriptor. nox_noun_identity evaluates its Noun argument once, then axis0 returns native balanced [[a b][c d]]. It never reuses tuple cons-list layout. Equality/type checks and any admitted Digest indexing match the seed; direct identity is needed before qualified imports. Structurally compare full 4-word output against independently loaded input particle, not a scalar projection.

Seed tuple syntax is unusual: `(T)` is a singleton tuple type; `()` and trailing comma in type are rejected. `(x)` groups an expression; values require at least two items, `(x,)` and `()` reject, `(x,y,)` accepts. Source contract must preserve these distinctions. Type/value parser continuations are bounded and own their child lists; comma must distinguish tuple context from function argument context. Each tuple is a cons-list of complete component values, ending in zero; Unit components retain their zero leaf and Noun components retain entire subtrees.

Destructuring evaluates RHS once into a hidden slot before any binding/write. New let names are installed only afterward. Assignment `(a,b)=(b,a)` reads both old values before ordered writes. `_` is admitted only in let patterns. Repeated names follow seed final-binding/ordered-write behavior. Check every target's type/mutability/arity before code emission; nested destructuring is unsupported unless seed syntax admits it. Tuple equality requires same descriptors and contains_noun=false, including nested components; Noun-containing tuples use explicit projections/native operations. Physical flat width is separate from native subtree slots.

## Acceptance and subsequent closure
Cover scalar/Noun/Unit tuples, nested tuples, typed functions, tuple returns, grouping vs tuple commas, shadowing, immutable targets, swapping/duplicate names, RHS traps/once-only evaluation and complete canonical outputs. Independent type/sequence/depth boundaries and raw seed oracle; old ART1 identities retained. Compile unchanged tree/seq/bytes tuple usage when dependencies/imports/structs are admitted, not rewritten approximations.

Follow with nominal structs/private fields and persistent nested field writes; fixed immutable Field arrays and checked indexing; constants/pub/attributes/assert semantics; true package import resolution/exact intrinsic declarations. SH4 must lift SOURCE_CAP4096, sentinels/pass bounds and actual arena scale before whole compiler closure. Then validate generated(1,1), compile C2 and C3, fixed point, six platforms and separate Zheng proof gates.

Constant-stage follow-up: add a seed regression for `module a; pub const X:Field=0` and `module b; use a; pub const Y:Field=a.X; pub fn f()->Field{Y}` (using actual newline syntax). Verify qualified constant initializers and aliases in their defining scope before guest constant resolution. Keep primitive type tags owned directly by types.tri.

## Tuple implementation ownership
`type_parse.tri` returns a descriptor and the first unread token using explicit bounded continuations; `types.tri` owns canonical data and never imports parsing. Group/tuple frames share the delimiter stack with calls/indexing, decoded only by their nearest marker. A group without commas keeps its original AST ID/span/formula. Tuple child links are owned reversed lists in the argument arena. Plain marker5 holds start/base; the first comma promotes it to marker38 with start/base/count/head; type children are gathered once from those links at close, avoiding repeated Seq decoding and preserving the old 64-group arena boundary. `tuple_gen` wraps reversed links into a zero-terminated cons-list, evaluating elements once left to right.

Flat pattern parsing retains exact name spans and recognizes `_` only in tuple let patterns. `tuple_bind` checks all assignment targets before reserving one invisible RHS slot, then emits internal LOCAL/PROJECT nodes and ordered WRITE records. Tuple axes2/6/14/... and Digest axes4..7 remain separate. Every hidden slot, expression and write consumes ordinary caps. New let names are visible only after the RHS is parsed; duplicate bindings/targets preserve seed last-name/ordered-write behavior. Bare `let _` and parenthesized single-name assignment retain their previous unsupported subset status in this increment. Test complete outputs, raw-seed positives/negatives, evaluation/traps and separate type/stack/node/depth/runtime boundaries before integration.


## Next nominal-record increments
Read seed `reference/language.md` (structs), `src/typecheck/file.rs`, `src/typecheck/resolve.rs`, `src/typecheck/expr.rs`, and `src/ir/tree/lower/nox/native/{expr,index}.rs` before extending the canonical subset. The seed now enforces the existing exactly-once initializer contract (`ac2481d`), including shorthand and nested constructors. Reject duplicate nominal declarations/fields explicitly in the bounded subset. Keep source-order named-type availability and reject recursive/forward layouts until deliberately supported.

Deliver module-local nominal descriptors, named annotations, constructors/shorthand and postfix field reads first; follow with persistent static nested writes, then import/visibility closure. Include defining logical module and full name plus field visibility now; cross-module privacy remains open until real imports execute. Use a field allowance independent of tuple arity (proposed32); validate unchanged compiler State definitions against it. One complete record occupies one native slot. Derive logical depth/node/contains-Noun metadata from registered fields; preserve nominal distinctions between equal layouts.

Struct literal expressions execute once in declaration order, even when initializer text is reordered (`reference/language.md`). Resolve named initializers before lowering; do not reuse tuple source-order links unchanged. Reads must work on variables, call results and parenthesized bases. Inside an owned group/call/index delimiter, allow struct literals even when the outer expression precedes an if/for block; a global before_block flag is insufficient (seed parser/expr.rs). Preserve uppercase final-name and named-field lookahead for constructor recognition. Writes later resolve the immutable typed field path and mutable root, evaluate RHS once, and rebuild the persistent path while preserving snapshots. Follow native/index.rs zipper semantics and chunk long paths, not one overflowing Field axis. Test empty/Unit/Noun/nested records, same-layout different names, missing/extra/duplicate fields, shorthand, private-owner boundaries, distinct traps proving declaration order, independent quotas, and all old ART1 identities. No whole-module/compiler closure claim before imports and SH4 scale.

Nominal data owner: `nominal.tri` constructs canonical owner/name plus ordered `{name, public, ty}` records; `types.info` reads a common metadata envelope without importing nominal parsing. Headers, type_parse and expression Context receive the registry explicitly. Never identify a type by alias, basename, temporary table ID or source span. Opaque Seq/Bytes fields remain private to their defining modules. Validate foreign-owner rejection through component APIs while imported-source acceptance remains open.


The first nominal delivery is descriptor construction/inspection and complete-name field lookup, with component tests for owner/visibility, empty records, arity32, invalid/duplicate names/types, logical sharing and depth bounds. Constructor result carries type, status0/5/7 and offending field index; common types.info recognizes the nominal metadata envelope, while tuple parts explicitly excludes nominal records. Source syntax and imports stay unaccepted until following deliveries connect the registry to headers/type_parse and expression constructors/field reads.

Seed availability detail: register struct fields and non-generic function signatures in source order; check all function bodies against the final immutable nominal registry (file.rs first pass then body pass). Later structs may appear in body annotations/constructors but not in earlier signatures or layouts. Keep `pub struct` in declaration/export records separately from field flags. Identifier copying/comparison must cover SOURCE_CAP, not ascii.same's legacy255 loop. Unknown named types become semantic5; `Unit` may be a user-defined struct name.
