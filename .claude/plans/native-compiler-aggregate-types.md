# SH3 native aggregate values
Feature PRs into release/0.4. Preserve primitive program ART1 identities and the raw/structured entry ABIs. Canonical descriptors, Digest, tuples/destructuring, module-local nominal source values, persistent field writes, fixed Field arrays and typed constants and assertions are accepted; attributes and real imports follow. These deliveries do not close SH3.

## Canonical descriptors — accepted

Internal foundation accepted at `adbb00e`; [validation](../../audit/self-hosting/native-compiler-types.md). Digest type, native identity, equality and checked Field/U32 indexing accepted at `dbf3c13`; [execution evidence](../../audit/self-hosting/native-compiler-digest.md). Tuple annotations/values, typed functions and ordered flat destructuring accepted at `850525c`; [validation](../../audit/self-hosting/native-compiler-tuples.md). Repeated struct initializers reject at `ac2481d`; [seed validation](../../audit/self-hosting/unique-struct-initializers.md). Nominal descriptor foundation accepted at `1e36ceb`; [validation](../../audit/self-hosting/native-compiler-nominal.md). Source declarations, constructors and field reads accepted at `60ea10e`; [validation](../../audit/self-hosting/native-compiler-records.md). Persistent static field writes accepted at `324a015`; [validation](../../audit/self-hosting/native-compiler-record-writes.md). Fixed Field arrays accepted at `a427531`; [validation](../../audit/self-hosting/native-compiler-arrays.md). Seed typed constants accepted at `2f6b0ef`; [validation](../../audit/self-hosting/typed-constants.md). Guest constants accepted at0051cd5; [validation](../../audit/self-hosting/native-compiler-constants.md). Assertions accepted at5730eb8; [validation](../../audit/self-hosting/native-compiler-assertions.md). Next: attributes, imports.
Represent types as canonical Noun values, with current primitive tags remaining atoms. Primitive AST/signature/binding records therefore preserve their bytes. Composite descriptors carry kind, ordered children, derived logical depth and contains_noun. Equality uses native identity of canonical descriptors; primitive tests compare with explicit tag atoms. No mutable type interner or O(n²) search, no evolving registry through every body.

Move type ownership into types.tri; migrate AST type access, signatures, bindings, body result and builtins from Field to Noun. Primitive tags have one owner in types.tri; packed records insert complete descriptors without atom wrapping. Primitive construction/equality uses direct noun intrinsics so the guest compiler avoids an extra function invocation per check. Invalid type has a unique descriptor distinct from all valid primitives/composites. Keep token recognition and all current accepted syntax unchanged in the initial refactor. Component tests assert primitive record byte preservation, exact descriptor equality and bounded composite construction. Full old JOB/ART1 corpus must preserve outputs/identities. Formally audited UNKNOWN is still not proof.

Composite constructors use bounded child sequences and explicit logical nesting/node limits derived from job capacity and parser STACK_CAP. Derived depth/contains_noun must come from trusted child descriptors; never accept caller-supplied metadata. Nominal struct identity later includes the defining module, not structural equivalence. Fixed arrays later carry element type and checked extent. Type bounds, output formula depth and execution arena remain independent.

## Digest and tuple language increments
Digest is its own primitive descriptor. nox_noun_identity evaluates its Noun argument once, then axis0 returns native balanced [[a b][c d]]. It never reuses tuple cons-list layout. Equality/type checks and any admitted Digest indexing match the seed; direct identity is needed before qualified imports. Structurally compare full 4-word output against independently loaded input particle, not a scalar projection.

Seed tuple syntax is unusual: `(T)` is a singleton tuple type; `()` and trailing comma in type are rejected. `(x)` groups an expression; values require at least two items, `(x,)` and `()` reject, `(x,y,)` accepts. Source contract must preserve these distinctions. Type/value parser continuations are bounded and own their child lists; comma must distinguish tuple context from function argument context. Each tuple is a cons-list of complete component values, ending in zero; Unit components retain their zero leaf and Noun components retain entire subtrees.

Destructuring evaluates RHS once into a hidden slot before any binding/write. New let names are installed only afterward. Assignment `(a,b)=(b,a)` reads both old values before ordered writes. `_` is admitted only in let patterns. Repeated names follow seed final-binding/ordered-write behavior. Check every target's type/mutability/arity before code emission; nested destructuring is unsupported unless seed syntax admits it. Tuple equality requires same descriptors and contains_noun=false, including nested components; Noun-containing tuples use explicit projections/native operations. Physical flat width is separate from native subtree slots.

## Acceptance and subsequent closure
Cover scalar/Noun/Unit tuples, nested tuples, typed functions, tuple returns, grouping vs tuple commas, shadowing, immutable targets, swapping/duplicate names, RHS traps/once-only evaluation and complete canonical outputs. Independent type/sequence/depth boundaries and raw seed oracle; old ART1 identities retained. Compile unchanged tree/seq/bytes tuple usage when dependencies/imports/structs are admitted, not rewritten approximations.

Follow with nominal structs/private fields and persistent nested field writes; fixed immutable Field arrays and checked indexing; constants/pub/attributes/assert semantics; true package import resolution/exact intrinsic declarations. SH4 must lift SOURCE_CAP4096, sentinels/pass bounds and actual arena scale before whole compiler closure. Then validate generated(1,1), compile C2 and C3, fixed point, six platforms and separate Zheng proof gates.

Constant-stage follow-up: add a seed regression for `module a; pub const X:Field=0` and `module b; use a; pub const Y:Field=a.X; pub fn f()->Field{Y}` (using actual newline syntax). Verify qualified constant initializers and aliases in their defining scope before guest constant resolution. Keep primitive type tags owned directly by types.tri.

## Tuple implementation ownership
`type_parse.tri` returns a descriptor and the first unread token using explicit bounded continuations; `types.tri` owns canonical data and never imports parsing. Group/tuple frames share the delimiter stack with calls/indexing, decoded only by their nearest marker. A group without commas keeps its original AST ID/span/formula. Tuple child links are owned reversed lists in the argument arena. Plain marker5 holds start/base; the first comma promotes it to marker38 with start/base/count/head; type children are gathered once from those links at close, avoiding repeated Seq decoding and preserving the old 64-group arena boundary. `aggregate_gen` wraps reversed links into a zero-terminated cons-list, evaluating elements once left to right.

Flat pattern parsing retains exact name spans and recognizes `_` only in tuple let patterns. `tuple_bind` checks all assignment targets before reserving one invisible RHS slot, then emits internal LOCAL/PROJECT nodes and ordered WRITE records. Tuple axes2/6/14/... and Digest axes4..7 remain separate. Every hidden slot, expression and write consumes ordinary caps. New let names are visible only after the RHS is parsed; duplicate bindings/targets preserve seed last-name/ordered-write behavior. Bare `let _` and parenthesized single-name assignment retain their previous unsupported subset status in this increment. Test complete outputs, raw-seed positives/negatives, evaluation/traps and separate type/stack/node/depth/runtime boundaries before integration.


## Next nominal-record increments
Read seed `reference/language.md` (structs), `src/typecheck/file.rs`, `src/typecheck/resolve.rs`, `src/typecheck/expr.rs`, and `src/ir/tree/lower/nox/native/{expr,index}.rs` before extending the canonical subset. The seed now enforces the existing exactly-once initializer contract (`ac2481d`), including shorthand and nested constructors. Reject duplicate nominal declarations/fields explicitly in the bounded subset. Keep source-order named-type availability and reject recursive/forward layouts until deliberately supported.

Deliver module-local nominal descriptors, named annotations, constructors/shorthand and postfix field reads first; follow with persistent static nested writes, then import/visibility closure. Include defining logical module and full name plus field visibility now; cross-module privacy remains open until real imports execute. Use a field allowance independent of tuple arity (proposed32); validate unchanged compiler State definitions against it. One complete record occupies one native slot. Derive logical depth/node/contains-Noun metadata from registered fields; preserve nominal distinctions between equal layouts.

Struct literal expressions execute once in declaration order, even when initializer text is reordered (`reference/language.md`). Resolve named initializers before lowering; do not reuse tuple source-order links unchanged. Reads must work on variables, call results and parenthesized bases. Inside an owned group/call/index delimiter, allow struct literals even when the outer expression precedes an if/for block; a global before_block flag is insufficient (seed parser/expr.rs). Preserve uppercase final-name and named-field lookahead for constructor recognition. Writes later resolve the immutable typed field path and mutable root, evaluate RHS once, and rebuild the persistent path while preserving snapshots. Follow native/index.rs zipper semantics and chunk long paths, not one overflowing Field axis. Test empty/Unit/Noun/nested records, same-layout different names, missing/extra/duplicate fields, shorthand, private-owner boundaries, distinct traps proving declaration order, independent quotas, and all old ART1 identities. No whole-module/compiler closure claim before imports and SH4 scale.

Nominal data owner: `nominal.tri` constructs canonical owner/name plus ordered `{name, public, ty}` records; `types.info` reads a common metadata envelope without importing nominal parsing. Headers, type_parse and expression Context receive the registry explicitly. Never identify a type by alias, basename, temporary table ID or source span. Opaque Seq/Bytes fields remain private to their defining modules. Validate foreign-owner rejection through component APIs while imported-source acceptance remains open.


The first nominal delivery (`1e36ceb`, accepted) is descriptor construction/inspection and complete-name field lookup, with component tests for owner/visibility, empty records, arity32, invalid/duplicate names/types, logical sharing and depth bounds. Constructor result carries type, status0/5/7 and offending field index; common types.info recognizes the nominal metadata envelope, while tuple parts explicitly excludes nominal records. Source syntax is connected at `60ea10e`; imports and cross-module source privacy remain open.

Seed availability detail: register struct fields and non-generic function signatures in source order; check all function bodies against the final immutable nominal registry (file.rs first pass then body pass). Later structs may appear in body annotations/constructors but not in earlier signatures or layouts. Keep `pub struct` in declaration/export records separately from field flags. Identifier copying/comparison must cover SOURCE_CAP, not ascii.same's legacy255 loop. Unknown named types become semantic5; `Unit` may be a user-defined struct name.

Source increment accepted at `60ea10e`: explicit Scope{owner,records} grows in headers and freezes for bodies. struct_parse owns declaration spans and only inserts complete layouts; type_registry copies full identifiers. record_frames/record_parse own constructor labels and initializer slots; emission orders values by declaration. aggregate_gen handles0..32 fields while type checking retains tuple16. Thread scope through all annotation/expression callers; preserve frames as first State field. Validate delimiter ownership, source-order signature/final-body availability, diagnostics, complete outputs, traps/once counts,32-field projection and all prior ART1 identities.

Static field-write increment accepted at `324a015`: use typed PROJECT→LOCAL chain; check mutable stable root slot and RHS type before append. Add RECORD_EDIT(lhs_id,rhs_id,root_type) and ordinary WRITE; no body_store/table ABI change. Rebuild from leaf upward with relative per-field axes (max2^33−2), preserving suffix and preceding siblings from original-frame prefix formulas; never concatenate a >62-bit axis. RHS occurs once; all sibling reads are pure. Count every AST/write and compute exact depth using the formula constructors. Test snapshots/siblings/frame widths1/2/3/5, old-value RHS, trace once, immutable/shadowed roots, constructor/call roots, whole Noun/Digest/tuple fields, paths61/62/63/64/>64 bits and exact caps/depth. Seed owners: typecheck/block.rs, stmts.rs, native/index.rs and nox/path.rs.

SH4 stress to retain in source-struct CLI receipt: type name S+256n, two fields256f+a/b, repeated in constructor and reads, remains valid but exhausts786432 nodes. Separate257-byte type-constructor and two distinct257-byte field-declaration programs pass. Never shorten identifiers to fit the arena; scale the compiler/runtime allowance in SH4 and keep complete-name semantics.

Field-write resource findings: adding the implementation made the prior nine-assignment/default-arena case exceed196608 nodes. Moving only body_state.State token/error ahead of cold context restored it before the following frame optimization; final measured costs are in the pinned receipt. Keep expression.State.frames first. Wide two-level records exceeded786432: record_frames revalidated2 sequences per finish and nominal.lookup decoded fields per label (97 validations for32 fields by static count). Constructor continuation now owns one immutable field list and raw indexed values tree, initialized with tree.empty(shape(arity).height), sentinel0/id+1. Decode nominal.fields and enforce all-field privacy once at enter; full-name lookup sets pending index/expected type; finish never forges opaque Seq handles. Validate exact old ART1s plus empty/trailing/duplicate/wide/nested constructors after this resource repair.

Unchanged wide source vectors (1118 bytes for61-bit path) still exhaust786432 during generation: direct Bytes+headers+check+plan succeeds, parser.compile fails even returning only error code. Keep all five61..65-bit full-source cases as explicit resource-boundary tests/CLI receipts; they are NOT positive full-C1 acceptance. Separate native_record_edit.tri/component tests execute complete persistent results and snapshots for61..65-bit axes plus63/64 PROJECT links, isolating codegen semantics from source scale. Positive full JOB cases cover nested/source callee/RHS-call/whole-record writes. SH4 must promote the retained wide source cases to positive acceptance. Rebuild depth calculation is exact: compose(parent,axis) has one fixed depth; every preceding cons adds2, eliminating repeated Value wrappers without changing formulas.

## Fixed Field-array increment — accepted
Closure inventory: emit.tri needs [Field;24] parameter/literals; lexer20, ascii12, builtins8/9, parser2, bytes4. No element writes, nested arrays or named extents in these owners. Seed owners: syntax/parser/{types,expr}.rs, typecheck/{expr,resolve}.rs, native/index.rs. Admit [Field;N] with raw decimal extent, [] as Field0, singleton/trailing comma, typed locals/parameters/results/tuple/record fields, whole assignment/equality and checked Field/U32 reads. Keep ARRAY identity distinct from Tuple/Digest. Reject unsupported symbolic/generic/compound extents; seed has no [x;N] repetition syntax.
Canonical ARRAY descriptor: element+length plus common metadata; Field depth1/nodes1+N, source quota before growth, types.parts excludes arrays. ARRAY_VALUE/ARRAY_INDEX AST, semicolon token, array_parse owns a distinct delimiter marker; extend index_parse without changing Digest. Emission uses owned reversed links and bounded chunks up to the existing sequence/logical cap; do not turn aggregate_gen's current32 loop into a new language limit.
Array READ literal OOB is a semantic failure on raw u64 spelling before Field reduction: [7][18446744069414584321] rejects, [7][18446744069414584321+0] reads0. Digest literal OOB remains a runtime trap. Index base executes before index, each once. Indexed writes remain outside this slice; expression-first parsing can encounter literal-read errors before seeing '=', so do not promise uniform unsupported6. Preserve this read/write asymmetry for the later place parser.
Use one reusable Get in the code table, installed only when a reachable body has an array read. Extend code_plan after functions/loops and guard function_gen singleton bypass; call_codegen/context must carry helper slot without changing old programs. Get descends a cons-list after bounds checking, avoiding axis62 and O(N) formula expansion. Check helper/table/depth budgets and unreachable-array-body identity. Values without reads need no helper. Whole arrays use existing local/call/return/record-write paths.
Acceptance: empty/singleton24/31/32/33, annotation/type/arity errors, tuple/struct/helper returns, first/last Field/U32 reads, zero length, literal/dynamic OOB and raw-u64 normalization. Owned delimiter mixes and malformed closings, element/base/index traps and once traces, whole-array snapshots/static field replacement, independent logical/AST/argument/operator/table/depth caps, complete emitted depth and all old ART1s. Keep arrays/imports/SH4 milestones distinct.


Array implementation on feat/0.4-native-field-arrays (base17f912b, PR84 merged): canonical tag18 Field arrays, raw-decimal extents, ARRAY_VALUE/ARRAY_INDEX, marker44 and semicolon43; owned reversed links emit bounded chunks. needs_get is monotone through all expression/body boundaries; packed body sizes carry [loop_count flag]. code_plan adds one helper after reachable functions/loops, with shared finish including last chunk. Get uses [table [0 [list index]]], axes14/15/28/29; source order once, runtime InvZero bounds, exact depth. No imports/constants/profile changes.
Array resource repair: move body statements/current forward; expression frames stays first with token second. Reuse already-read local/assignment lookahead tokens. body_store.read now returns compact Body(nodes,args,statements,blocks,loops,root,returns,needs_get), retaining complete public Seq validation; source-only parser context ends at pack. This restores old9-assignment/64group and function-depth component budgets. Accepted at a427531:1048/122/380 tests,133 unchanged fixture rows/43 baselines,851 installed commands/288 observations,141 positive cases with all127 old ART1s identical;79 formal UNKNOWN. All rebuilt binary and C1 bytes identical. Semicolon outside array types retains unsupported6, including repetition syntax.
New component table test covers final4096-function chunk, independently of current source/graph size. Rust prepares canonical IDs; fixture constructs repeated checked bodies and uses public Seq admission. Its explicit larger component arena is not the JOB/default arena; pinned validation reports separate actual costs and12,582,912node/1bn-reduction allowances. Full-source corpus caps stay unchanged. New positives compare with native raw seed, wrapping scalar main through a helper, since legacy assembly dynamic indexing is unsupported. Test type/source/delimiter/table/depth boundaries, evaluation order and complete33-element output; prior array rejection cases become acceptance. Remaining SH4 wide source/resource cases retained unchanged.


Seed typed constant resolution accepted at2f6b0ef; [validation](../../audit/self-hosting/typed-constants.md). Checker/signatures/exports, generics, native nox and TIR now share final active typed bindings; aliases, raw dimensions, visibility and invalid initializers have common semantics. Guest constants follow the [next implementation plan](seed-typed-constants.md), then attributes/asserts and true import ownership.
