# Guest module graph — next source unit
Prerequisites: retained Package/BytesTable in bbf19fa; strict shared seed paths prepared in fix/0.4-strict-module-paths. Keep compiler/Joy/Trisha feature deliveries on release/0.4, defaults untouched. Canonical contracts first; graph components alone do not close guest imports or SH3/SH4.
Header component tests pass, including invalid boundary tokens: exact program/module owner and one use at a time; lexer tokens preserve comments/whitespace, normalized temporary Bytes excludes them. Retain use_start separately from path_start/path_end and the next offset. IDENT is mandatory after every dot. Exact requested/canonical owner mapping remains explicit; no host discovery or concatenation.
State: separate typed BytesTables for reached sources and canonical names; compact module records store original package index, body offset, ACTIVE/DONE, use count and ordered/reversible raw use records. A compiler-owned canonical raw tree indexed by package index stores0/unseen or compact ID+1; initialize with shared zero subtrees instead of65536 pushes. Never reuse4096 as a package-index sentinel.
Discovery: iterative source-order DFS, bounded short chunks and explicit parent frames. Register before descent; ACTIVE detects cycles, DONE shares diamonds. Cache entry View.source/name. Open and UTF-8-check each newly reached source once, subtract bytes from shared4096 first, preserve remaining Package allowance and explicit OPT1. Unused raw garbage is never lexed.
Ordering: keep every use occurrence/source order for per-symbol final bindings. Seed topology is lexical root order followed by source-order dependency DFS, not lexical-ready Kahn or entry-only discovery postorder. Produce topology separately; a sparse pending-root tree can select/remove the least remaining package index without scanning65536 slots. Dedup graph traversal only, never the binding list.
Bounds/diagnostics: cap modules, total uses and frames against sequence_length/common source-work limits before append; raw lists cannot bypass quotas. Emit4 needs its own cycle message. Missing/cycle errors identify the caller's original use span; owner mismatch identifies the reached declared header; malformed UTF-8 is1 before parsing; incomplete paths are2; capacity7 retains original package index.
Implemented graph/order; component tests pass (4 graph +3 header), including independently host-admitted JOBs, package index4096 and shared source4096/4097. Accepted integration096f68c:1130/122/380 tests,133 unchanged baselines,98 formal UNKNOWN,39 graph CLI commands/12 observations plus35 strict-path commands/12 observations pass; entire C1, component outputs and all3 post-commit binaries unchanged. Evidence audit/self-hosting/guest-module-graph.md; feature delivery to release/0.4 follows. CLI explicitly selects existing --modules65536 and --time-ms60000; public defaults unchanged. Acceptance: entry basename, dotted/commented headers, repeated a,b,a uses, diamond once-only admission, lexical-vs-DFS counterexample, missing/cycle/mismatch/reachable invalid UTF-8/unused invalid bytes, index4096, shared source/sequence/allowance exact and one-below. First end-to-end imported public constant follows graph components; foreign constant tokens never read caller source. Legacy remaps are explicitly open; canonical import spellings only in this component. Then qualified functions/types/intrinsics, generated1/1 profiles, full closure/scale, C2/C3 and six platforms; native Zheng proof gates remain separate.
