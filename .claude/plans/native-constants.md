# Native constants — accepted

Source0051cd5; [validation](../../audit/self-hosting/native-compiler-constants.md). Base a7096d4 (PR86 seed constants merged; tree b6b9ea9). Branch feat/0.4-native-constants; root/cache/install selfhost-0.4-constants. Master unchanged. Contract reference/self-hosting.md precedes implementation. See seed-typed-constants.md for decisions and later assertions/attributes/imports.

Implement constants/const_parse, header EOF resolution, immutable body/expression scope, distinct CONSTANT AST and coverage/codegen. Explicit iterative memoization with every declaration checked; full name identity; typed raw U32; grouped initializer; cap independent from AST/type/frames. No new full source pass, resource quota increase or weakened old vector. Scope owns module once; alias references retain literal provenance. Preserve prior ART1 output.

Validate focused Rust guest corpus and component bounds, installed Joy entire CLI corpus incl old SH4 failures, all three owners' checks/tests, 133 fixture rows/43 baselines and formal audit (UNKNOWN remains open). Review passes1,2,5,6,7,8,9,10. Commit source then rebuild/install all three; evidence commit then repeat. PR targets release/0.4 with exact tested tree. Noun temperature128K until full milestones close.

Next deliveries: assert/assert_eq and resolved halting first, then pure/requires/ensures, before cfg/imports. Both were read-only reviewed. attributes.tri prefix scanner before pub; const_parse.boundary admits next #. Signature stores EACH declaration's pure flag, including replaced functions; carry into body/expression calls. Purity checks direct basename before callable resolution, exactly src/typecheck/builtins.rs::is_io_builtin (including ordinary same-named functions); no transitive check.
Seed src/syntax/parser/items.rs::parse_attribute stores token-text metadata, balances only parentheses, allows empty/unknown/malformed-expression text, then requires ]. Do not expression-typecheck or execute contracts. Unknown attributes/cfg/test/intrinsic unsupported6; fn-only attributes on const/struct reject. Invalid lexical tokens/u64 overflow1; EOF/unclosed delimiter2. Exact asm inside contract stays unsupported6 until its special lexer exists; other lexer-valid token13 metadata cannot all reject. No artificial64 expression-stack cap on metadata counter.
ASSERT Bool->Unit, ASSERT_EQ Field×Field->Unit, explicit before broad NOUN arity/type ranges and codegen fallbacks. Emission branch(arg,q0,InvZero(q0)) and branch(Eq(lhs,rhs),q0,InvZero(q0)); ordered once-only evaluation, no code-table helper. Depth max(arg,3)+2; equality max(max(lhs,rhs)+2,3)+2.
Shared halting helper recognizes resolved ast.BUILTIN/ASSERT with sole ast.BOOLEAN value1; grouped false remains same node. Ordinary fn assert is ast.CALL. Body RETURN type exemption and direct EVAL→returned; terminal tail type exemption; coverage EVAL and any false-assert tail, including nonterminal arms. Do not propagate body.returned out of IF/loop: seed intentionally permits defensive continuation. assert_eq(0,1),assert(1==0),let x=assert(false) do not provide coverage. Tests: metadata-only ART1 identity even false/unknown/empty contracts; malformed attrs, reset between functions/replacements; pure direct shadow I/O vs transitive; exact arity/types; shadow assertions; nested/loop halting; typed return/tail; trap order/once; exact complete formula depth and unchanged old196608 cases.

Implementation resource repair: Header.State token/error first, Expression.State frames/token/ops/operators/nodes first; owner enters constants scope only at first declaration. Reuse existing typed syntax.ZERO throughout compiler instead of repeated checked casts. std.nox.tree shape specializes<=32 without length-1 narrowing; get/set singleton and push empty return directly AFTER original bounds assertions. Canonical layout and validation visit accounting unchanged; new tests compare height/capacity separately at every U32 power boundary and pair-leaf/snapshot/bounds behavior. Old native_function_depth fixture/source/196608 cap retained; compact header scanning and byte-power lookup experiments were discarded. Temporary larger-arena probes are removed; acceptance uses original budgets. 1066/122/380 tests pass,133 fixture rows/43 baselines unchanged;965 installed commands/326 observations retain all141 prior positive ART1 identities. All83 formal reports UNKNOWN. Post-source-commit installs reproduce all binaries and C1. Next assertions are isolated in selfhost-0.4-assertions, feat/0.4-native-assertions; attributes follow.
